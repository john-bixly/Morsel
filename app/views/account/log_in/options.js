define(['text!accountView/log_in/template.html'], function (template) {
    'use strict';

    return {
        appendTo : '#log_in',
        template : template,
        rivetsInstaUpdate : false,
        modelData : {

        }
    };
});