define(['text!accountView/register/template.html'], function (template) {
    'use strict';

    return {
        appendTo : '#register',
        template : template,
        rivetsInstaUpdate : false,
        modelData : {

        }
    };
});