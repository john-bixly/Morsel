define(['text!searchView/template.html'], function (template) {
    'use strict';

    return {
        appendTo : '#stage',
        template : template,
        rivetsInstaUpdate : true,
        modelData : {
           
        }
    };
});